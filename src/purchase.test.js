const { createPurchase, deletePurchase, updatePurchase } = require('./purchase');

const testData = [
  { id: 'abc123', name: 'Item 1', amount: 10.5, donation: 0 },
  { id: 'def456', name: 'Item 2', amount: 15.75, donation: 0 },
  // Add more sample items as needed
];

test('Create Purchase - Creates a new item and appends it to the test array', () => {
  // Test case for createPurchase
});

test('Delete Purchase - Deletes the correct object in the test array', () => {
  // Test case for deletePurchase
});

test('Update Purchase - Edits the correct object in the test array', () => {
  // Test case for updatePurchase
});


